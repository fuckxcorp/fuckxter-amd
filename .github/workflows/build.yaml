name: I'm living all alone because

on:
  push:
  workflow_dispatch:
jobs:
  build:
    name: Keep running back, coming back for more
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Build!
        run: |
          set -o pipefail

          xcodebuild \
            -project FuckXter.xcodeproj \
            -scheme FuckXter \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            clean build
          
          APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData \
            -path "*/Build/Products/Debug-iphonesimulator/FuckXter.app" \
            -print -quit)
          
          ditto -c -k --sequesterRsrc --keepParent \
            "$APP_PATH" ./fuckxter.app.zip

      - name: Release
        uses: softprops/action-gh-release@master
        with:
          tag_name: v0.0.1
          files: ./fuckxter.app.zip
          body: |
            # 📝 Release Notes
            - Fixes and improvements
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}